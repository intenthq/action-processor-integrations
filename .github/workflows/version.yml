name: Version

on:
  workflow_dispatch:
    branches:
        - master
          
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: olafurpg/setup-scala@v2
      - name: Show Version
        run: sbt version
